import styled from "styled-components";
import React from "react";

const RegisterPageBlock = styled.div``;

const RegisterPage = () => {
  return <RegisterPageBlock>회원가입 페이지</RegisterPageBlock>;
};

export default RegisterPage;
